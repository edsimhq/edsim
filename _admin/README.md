# _admin

Internal files — not linked from the public site.

## Contents
- `build-standards.md` — authoritative standards for all EdSim sims
- `templates/` — starter templates for new sims (coming)

## Repo structure
```
edsim/
├── index.html                     ← public homepage / catalog
├── _admin/                        ← this folder
└── us-history/
    └── {unit}/
        └── {sim-type}/
            └── index.html         ← the sim, served at edsim.org/us-history/{unit}/{sim-type}/
```
